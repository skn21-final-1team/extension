# Bookalpie 사용 가이드 v3.0.2

Chrome 북마크를 쉽고 빠르게 관리하는 Bookalpie 확장 프로그램의 사용법입니다.

---

## 목차

1. [설치하기](#1-설치하기)
2. [기본 화면 구성](#2-기본-화면-구성)
3. [북마크 관리](#3-북마크-관리)
4. [폴더 관리](#4-폴더-관리)
5. [검색](#5-검색)
6. [드래그 앤 드롭](#6-드래그-앤-드롭)
7. [선택 및 일괄 작업](#7-선택-및-일괄-작업)
8. [퀵 세이브](#8-퀵-세이브)
9. [테마 변경](#9-테마-변경-다크라이트)
10. [Kalpie Notebook 동기화](#10-kalpie-notebook-동기화)
11. [단축키](#11-단축키)
12. [FAQ](#12-자주-묻는-질문-faq)

---

## 1. 설치하기

### Chrome Web Store에서 설치 (권장)

1. Chrome Web Store에서 **"Bookalpie"** 검색
2. **"Chrome에 추가"** 클릭
3. 팝업에서 **"확장 프로그램 추가"** 클릭
4. Chrome 툴바 우측 퍼즐 아이콘(🧩) 클릭 → Bookalpie 고정(📌)

### 개발자 모드로 설치

```bash
# 1. 프로젝트 클론 후 빌드
cd extension
pnpm install
pnpm build

# 2. Chrome에서 로드
# chrome://extensions 접속
# "개발자 모드" 토글 ON
# "압축해제된 확장 프로그램을 로드합니다" 클릭
# dist/ 폴더 선택
```

---

## 2. 기본 화면 구성

Chrome 툴바에서 Bookalpie 아이콘을 클릭하면 팝업이 열립니다.

```
┌─────────────────────────────────┐
│  🔖 Bookalpie       ☀️  ☁️  ✕  │  ← 헤더 (로고, 테마, 동기화, 닫기)
├─────────────────────────────────┤
│  🔍 검색창              ⬆  ⬇   │  ← 검색 + 전체 접기/펼치기
├─────────────────────────────────┤
│  📁 북마크 바                    │
│    📄 자주 가는 사이트            │
│  📁 기타 북마크                  │  ← 폴더 트리 (VS Code 스타일)
│    📁 개발                      │
│      📄 GitHub                  │
│      📄 Stack Overflow          │
│    📁 뉴스                      │
│      📄 네이버                   │
├─────────────────────────────────┤
│ ☐  ⚡빠른저장  📌현재탭  📁폴더  🔗URL추가 │ ← 하단 액션 바
├─────────────────────────────────┤
│  (폼 패널 / 동기화 설정)          │  ← 필요 시 펼쳐지는 하단 패널
└─────────────────────────────────┘
```

- **헤더**: 로고, 테마 전환(다크/라이트), 동기화 설정, 닫기 버튼
- **검색 바**: 북마크 검색 (300ms 디바운싱) + 전체 접기/펼치기 버튼
- **폴더 트리**: 모든 북마크를 계층적으로 표시 (드래그 앤 드롭 지원)
- **하단 액션 바**: 전체 선택, 빠른저장, 현재 탭 저장, 폴더 생성, URL 추가
- **하단 패널**: 폼 입력 또는 동기화 설정이 펼쳐지는 영역

---

## 3. 북마크 관리

### 현재 탭 저장

1. 하단 액션 바에서 **"현재탭"** 버튼 클릭
2. 현재 열린 탭의 제목과 URL이 자동으로 채워집니다
3. 저장할 **폴더를 선택**
4. **"저장"** 클릭

### URL 직접 추가

1. 하단 액션 바에서 **"URL 추가"** 버튼 클릭
2. 제목과 URL 입력
3. 저장할 **폴더를 선택**
4. **"추가"** 클릭

### 폴더에서 바로 추가

1. 폴더에 마우스를 올리면 우측에 **"+"** 아이콘 표시
2. **"+"** 클릭 → 해당 폴더에 북마크 추가 폼 열림
3. 제목과 URL 입력 후 **"추가"** 클릭

### 북마크 수정

1. 북마크에 마우스를 올리면 우측에 **연필(✏️)** 아이콘 표시
2. 연필 아이콘 클릭 → 미니 편집 카드 열림
3. 제목, URL 수정 후 **"저장"** 클릭

### 북마크 삭제

1. 북마크에 마우스를 올리면 우측에 **휴지통(🗑️)** 아이콘 표시
2. 휴지통 아이콘 클릭
3. 확인 다이얼로그에서 **"삭제"** 클릭

### 북마크 열기

- **클릭**: 현재 탭에서 열기
- **Ctrl + 클릭** (Mac: Cmd + 클릭): 새 탭에서 백그라운드로 열기

---

## 4. 폴더 관리

### 새 폴더 만들기

1. 하단 액션 바에서 **"폴더"** 버튼 클릭
2. 폴더 이름 입력
3. 상위 폴더 선택 (최상위에 만들려면 "최상위" 선택)
4. **"만들기"** 클릭

### 폴더 이름 변경

- **방법 1**: 폴더 이름을 **더블클릭** → 인라인 편집 모드 → Enter로 저장
- **방법 2**: 폴더에 마우스를 올려 **연필(✏️)** 아이콘 클릭 → 인라인 편집

### 폴더 삭제

1. 폴더에 마우스를 올리면 우측에 **휴지통(🗑️)** 아이콘 표시
2. 휴지통 아이콘 클릭
3. 확인 다이얼로그에서 **"삭제"** 클릭

> **주의**: 폴더를 삭제하면 안에 있는 모든 북마크와 하위 폴더도 함께 삭제됩니다.

---

## 5. 검색

1. 상단 **검색창**에 키워드 입력
2. 폴더 이름, 북마크 제목, URL을 필터링 (300ms 디바운싱 적용)
3. 검색 중에는 모든 폴더가 자동으로 펼쳐집니다
4. 검색창의 **X** 버튼으로 검색 초기화

### 전체 접기 / 펼치기

- 검색창 옆 **⬇ (펼치기)** 버튼: 모든 폴더 열기
- 검색창 옆 **⬆ (접기)** 버튼: 모든 폴더 닫기

---

## 6. 드래그 앤 드롭

마우스로 북마크나 폴더를 끌어서 자유롭게 이동할 수 있습니다.

### 북마크 이동

1. 북마크를 **클릭한 채로 드래그**
2. 원하는 위치로 이동
   - **같은 폴더 내**: 순서 변경
   - **다른 폴더 위**: 해당 폴더로 이동 (폴더가 하이라이트 됨)
3. 마우스 버튼을 놓으면 이동 완료

### 폴더 이동

1. 폴더를 **클릭한 채로 드래그**
2. 다른 폴더 안으로 또는 원하는 순서로 이동
3. 마우스 버튼을 놓으면 이동 완료

> **참고**: 폴더를 자기 자신의 하위 폴더로 이동할 수는 없습니다 (순환 참조 방지).

---

## 7. 선택 및 일괄 작업

### 개별 선택

- 북마크나 폴더 좌측의 **체크박스(☐)** 클릭
- 폴더를 선택하면 하위 항목도 함께 선택됩니다

### 전체 선택 / 해제

- 하단 액션 바에서 **"전체선택"** 클릭 → 모든 항목 선택
- 선택된 상태에서 **"X" (선택 해제)** 클릭 → 모든 선택 해제
- 선택 개수가 표시됩니다 (예: "3개 선택됨")

### 일괄 삭제

1. 삭제할 북마크/폴더를 체크박스로 선택
2. 하단 액션 바의 **휴지통(🗑️)** 버튼 클릭
3. 확인 다이얼로그에서 **"삭제"** 클릭

---

## 8. 퀵 세이브

자주 사용하는 폴더를 지정해두면, 한 번의 클릭으로 현재 탭을 저장할 수 있습니다.

### 퀵 세이브 폴더 설정

1. 하단 액션 바에서 **"⚡ 빠른저장 폴더 설정"** 선택
2. 원하는 폴더를 선택
3. **"설정"** 클릭

### 퀵 세이브 사용

1. 설정이 완료되면 상단 또는 액션 바에 **⚡ 퀵 세이브 버튼** 표시
2. 버튼 클릭 → 현재 탭이 지정 폴더에 바로 저장
3. 상태 표시:
   - **"저장중..."** — 저장 진행 중
   - **"저장됨!"** — 저장 완료 (초록색)
   - **"실패"** — 저장 실패 (빨간색)

### 퀵 세이브 폴더 변경

- 퀵 세이브 버튼을 **우클릭** → 폴더 변경 폼 열림
- 또는 설정에서 **"초기화"** 후 다시 설정

> **참고**: 퀵 세이브로 지정한 폴더가 삭제된 경우, 다음 퀵 세이브 시 자동으로 설정이 해제됩니다.

---

## 9. 테마 변경 (다크/라이트)

- 상단 우측의 **🌙 (달)** 아이콘 클릭 → 다크 모드
- 상단 우측의 **☀️ (해)** 아이콘 클릭 → 라이트 모드
- 설정은 자동 저장되어 다음에도 유지됩니다

---

## 10. Kalpie Notebook 동기화

선택한 북마크를 Kalpie Notebook 서비스로 전송하여 AI 기반 질의응답에 활용할 수 있습니다.

### Sync Key 설정

1. 상단 우측의 **☁️ (구름)** 아이콘 클릭 → 동기화 설정 패널 열림
2. Kalpie Notebook에서 발급받은 **Sync Key** 입력
3. Key 입력 후 저장

### 북마크 동기화

1. 전송할 북마크/폴더를 **체크박스로 선택**
2. **☁️ 동기화 설정** 패널 열기
3. 선택된 항목 확인 (폴더 수, 북마크 수 표시)
4. **"전송"** 클릭
5. **개인정보 동의** (최초 1회만)
   - 선택한 북마크만 전송됨
   - 언제든 삭제 가능
   - HTTPS 보안 통신 사용
   - **"동의하고 계속하기"** 클릭
6. 전송 완료 시 성공 메시지 표시

### 동기화 취소

- 전송 중에 **"취소"** 버튼을 클릭하면 즉시 중단됩니다

### Sync Key 발급 방법

1. [Kalpie Notebook](https://bookalpie.com) 웹사이트 접속
2. 로그인 후 **설정 → 확장 프로그램 연동** 메뉴
3. **"키 발급"** 클릭하여 Sync Key 복사
4. Bookalpie 확장 프로그램에 붙여넣기

---

## 11. 단축키

| 동작 | 단축키 |
|------|--------|
| 북마크 열기 (현재 탭) | 클릭 |
| 북마크 열기 (새 탭, 백그라운드) | Ctrl + 클릭 (Mac: Cmd + 클릭) |
| 폴더 이름 변경 | 더블클릭 |
| 폼 입력 확인 | Enter |
| 폼 닫기 / 취소 | Esc |

---

## 12. 자주 묻는 질문 (FAQ)

### Q: 북마크가 안 보여요

Chrome의 북마크가 Bookalpie에 자동으로 표시됩니다. 북마크가 없다면 Chrome에서 먼저 북마크를 추가해주세요.

### Q: 드래그 앤 드롭이 안 돼요

폴더나 북마크를 클릭한 후 잠시 기다렸다가 드래그해주세요. Chrome 시스템 폴더(북마크 바, 기타 북마크)는 이동할 수 없습니다.

### Q: Sync Key는 어디서 발급받나요?

[Kalpie Notebook](https://bookalpie.com) 웹사이트에서 로그인 후 발급받을 수 있습니다. 자세한 방법은 [동기화 섹션](#10-kalpie-notebook-동기화)을 참고하세요.

### Q: 동기화하면 어떤 정보가 전송되나요?

**선택한 항목만** 전송됩니다:
- 폴더 이름
- 북마크 제목
- 북마크 URL

개인정보(비밀번호, 쿠키 등)는 **절대 전송되지 않습니다**.

### Q: 다크 모드 설정이 초기화돼요

다크 모드 설정은 브라우저의 localStorage에 저장됩니다. 브라우저 데이터를 삭제하면 초기화될 수 있습니다.

### Q: 삭제한 북마크를 복구할 수 있나요?

Bookalpie에서 삭제한 북마크는 Chrome의 북마크에서도 삭제됩니다. 삭제 전 항상 확인 다이얼로그가 표시되니 신중하게 결정해주세요.

### Q: 퀵 세이브 폴더를 변경하고 싶어요

퀵 세이브 버튼을 **우클릭**하면 폴더 변경 폼이 열립니다. 또는 설정에서 초기화 후 다시 설정할 수 있습니다.

---

## 문의 및 피드백

- GitHub: [https://github.com/skn21-final-1team/extension](https://github.com/skn21-final-1team/extension)
- 문제가 있으면 GitHub Issues에 등록해주세요.

---

**Bookalpie v3.0.2** | Kalpie와 함께 북마크를 스마트하게 관리하세요.
